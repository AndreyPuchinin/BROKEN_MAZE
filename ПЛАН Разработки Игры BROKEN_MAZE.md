ПЛАН Разработки ИГРЫ BROKEN_MAZE

Этапы:
Каждый этап - отдельный файл
BROKEN_MAZE_GAME.html - главный файл, который содержит базовую HTML/CSS разметку, темную тему, структуру интерфейса и подключает все этапы (Этап 1 и заглушки).
Stage1.js - содержит всю математику, алгоритмы генерации, структуру данных и отрисовку в консольном (ASCII) виде.
Таким образом можно делать заглушки
Заглушки не должны ломать работу программы!

Этап 1: Математика и Генерация (Headless / Логика)
1. Самый важный этап. Без него 3D не будет работать.
2. Реализация структуры данных для треугольной сетки (координаты `x, y, floor`, 6 соседей).
3. Написание алгоритма генерации: Остовное дерево (Прим/DFS) -> Добавление циклов -> Добавление лестниц -> Изъятие и замена ребер.
4. Размещение Старта и Финиша с учетом метрик (BFS для графового расстояния, Евклидово для геометрического).
5. Для быстрой отладки этапа временно заменим вывод треугольной карты по принципу ТЦ - на консольный вывод одного этажа моноширинным шрифтом.
6. Навигация по этажам: колесико мыши вверх - следующий этаж, колесико мыши вниз - предыдущий.
7. Надпись над Полем игры с консольным временным выводом "Используйте колесико мыши для навигации между этажами".
8. Если лабиринт не вмещается во внешние границы консоли-Поля игры, у него возникает внутренний размер и бегунки справа и снизу.
9. В консольном режиме отладки есть:
   9.a. Поле игры
   9.b. Поле HUD
   9.c. Поле настроек
      9.c.i. Для каждого этажа — отдельное поле (не шкала!) % покрытия ребрами.
      9.c.ii. Для каждого этажа — отдельное поле (не шкала!) % покрытия циклами.
      9.c.iii. Для каждой пары соседних этажей (1↔2, 2↔3 и т.д.) — отдельное поле (не шкала!) % лестниц.
10. Отрисовка лабиринта в консоль-Поле игры:
    10.a. Если в ячейке есть лестница, в ней рисуется:
       10.a.i. оранжевая `↑`, если лестница только вверх
       10.a.ii. оранжевая `↓`, если лестница только вниз
       10.a.iii. оранжевая `⭥`, если лестницы и вверх, и вниз
       10.a.iv. оранжевая `●`, если в ячейке нет лестниц
       10.a.v. синяя `S` / красная `F`, если в ячейке старт/финиш (даже если там лестница - старт и финиш важнее!)
       10.a.vi. серые `-`, `/` или `\`, если между узлами есть коридор
       10.a.vii. (пробел), если между узлами нет коридора
       10.a.viii. Пример:
===Этаж 0=== (рисуется посередине красным)
S - ↑   ●
 \   \ / 
  ● - ⭥   ↓
     /   / 
    ●   ● - F
    10.b. Узлы чередуются в соседних строках, благодаря нарастанию длины пробельного префикса в начале каждой строки:
       10.b.i. Первая строка: префикс = «» (0 пробелов)
       10.b.ii. Вторая строка: префикс = « » (1 пробел)
       10.b.iii. Третья строка: префикс = «  » (2 пробела)
       10.b.iv. И т.д.
       10.b.v. Весь текст лабиринта центрируется и по вертикали, и по горизонтали.
       10.b.vi. По горизонтали - так, как если бы в каждой строке было бы столько же символов, сколько в последней
    10.c. (Для отладки этапа)
    10.d. Нужно Поле настроек - через него мы задаем данные уровня
    10.e. Нужно Поле игры - в нем мы видим:
       10.e.i. Узлы
       10.e.ii. Рёбра
       10.e.iii. Этажи
       10.e.iv. Координаты старта
       10.e.v. Координаты финиша
       10.e.vi. Расстояние от старта до финиша в заданной метрике
       10.e.vii. Выбранная метрика

Результат этапа: Консольный вывод готового графа, проверка связности и корректности расстояний.

Этап 2: Режим отладки (Debug) и Полировка
1. Активация режима по читкоду.
2. Рендер вида «как на карте ТЦ» (слои этажей, сетка, лестницы, путь до финиша).
3. Переключение этажей колесиком мыши в отладке.
4. Оптимизация (ленивая загрузка, ограничение FPS), head bobbing (покачивание), адаптивность.

Результат этапа: Готовый продукт, соответствующий 100% ТЗ.

Этап 3: 3D Рендер и «Визуальная иллюзия» (Three.js)
1. Самый сложный визуальный этап.
2. Преобразование координат: маппинг треугольных координат (60°) в визуальные квадратные (90°).
3. Генерация 3D-меша: стены, полы, потолки. Раскраска коридоров (п. 3.1.3).
4. Настройка камеры от первого лица.
5. Реализация дискретного движения (snap-to-node) и плавной анимации поворота на 90°.
6. Логика «Лево-центричного» и «Право-центричного» режимов (п. 3.1.2).

Результат этапа: Можно «ходить» по сгенерированному лабиринту в 3D, видны искажения и цвета.

Этап 4: Управление и Маппинг направлений
1. Обработка ввода с ПК (WASD, блокировка ввода во время анимации).
2. Реализация виртуальных джойстиков для тач-устройств (п. 1.3.2).
3. Логика взаимодействия с лестницами (подъем/спуск).

Результат этапа: Полностью играбельный геймплей на ПК и мобильных.

Этап 5: UI, HUD и Настройки
1. Верстка меню настроек (шестеренка), панели паузы.
2. Динамическая генерация полей настроек (слайдеры для этажей, лестниц).
3. Реализация HUD (таймер, шаги, % повторов, этаж).
4. Сохранение/загрузка прогресса и настроек в `localStorage`.

Результат этапа: Полноценный интерфейс, игра сохраняется между сессиями.